id: lingbot-vision-vit-large
upstream:
  hf_repo: robbyant/lingbot-vision-vit-large
  revision: 5e0370623d4fa5db945d00bc47a8545eed407d6b
  license: apache-2.0
  license_terms: permissive
  pipeline_tag: image-feature-extraction
  size_bytes: 1213041759
conversion:
  tool: models/lingbot/export.py
  min_tool_version: 0.4.1
  args:
    variant: large
    size: 512
  quantization: none
  precision: float32
expected:
  format_version: '2.0'
  bundle_files:
  - metadata.json
  - main.mlirb
  - main.hash
parity:
  gate_a:
    checks:
    - bundle_files_present
    - metadata_json_parses
    - metadata_matches_recipe
  gate_b:
    metric: graph_output_cosine
    threshold: 0.999
    tolerance: 0.0005
publish:
  hf_target_namespace: kevinqz
  repo_name: LingBot-Vision-ViT-Large-CoreAI
  collection: CoreAI · Apple on-device
catalog:
  name: LingBot-Vision ViT-L (fabric)
  family: LingBot
  bundle_kind: image-feature-extraction
  min_os:
    macos: '27.0'
    ios: '27.0'
  runtime_facts:
    stock_runtime: false
    custom_kernel: false
    patch_required: false
    aot_required: false
  capabilities:
  - image-feature-extraction
  modalities:
    input:
    - image
    output:
    - embedding
  parameters: 0.3B
  architecture: transformer
  runner: CoreAIRunner
  tokenizer_required: false
  processor_required: true
status: registered
notes: 'LingBot-Vision ViT-L/16 (robbyant/lingbot-vision-vit-large @ 5e037062, apache-2.0)
  — a self-supervised vision backbone for dense spatial perception (depth / segmentation features).
  Frozen ViT-L/16 (embed_dim 1024); same lane as lingbot-vision-vit-small (SDPA attention,
  op-coverage proven). Exported via models/lingbot/export.py as ONE static-size .aimodel:
  image [1,3,512,512] -> normalized per-patch tokens [1, 1024, 1024]. fp32; the host owns
  image preprocessing (resize to 512, ImageNet mean/std, patch 16). GATE B = graph_output_cosine
  (identical seeded images through the fp32 torch backbone vs the .aimodel, min cosine over
  n_obs=8) — conversion fidelity, not task accuracy. Upstream model class + weights via github.com/robbyant/lingbot-vision
  (only model.pt on HF). Real conversion via models/lingbot/{export,parity}.py.'
published:
  hf_repo: kevinqz/LingBot-Vision-ViT-Large-CoreAI
  revision: e5c8db6081835d34c047785d3ceb60a95e0973c0
  date: '2026-07-07'
catalog_pr: https://github.com/kevinqz/coreai-catalog/pull/25
