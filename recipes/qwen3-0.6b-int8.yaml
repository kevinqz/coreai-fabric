id: qwen3-0.6b-int8
upstream:
  hf_repo: Qwen/Qwen3-0.6B
  revision: c1899de289a04d12100db370d81485cdf75e47ca
  license: apache-2.0
  license_terms: permissive
  pipeline_tag: text-generation
  size_bytes: 4522815806
conversion:
  tool: coreai.llm.export
  compression_config: quant/int8_absmax_perblock32.yaml
  min_tool_version: 0.4.1
  args:
    platform: macOS
  quantization: int8
  precision: float16
expected:
  format_version: '2.0'
  bundle_files:
  - metadata.json
  - main.mlirb
  - main.hash
parity:
  gate_a:
    checks:
    - bundle_files_present
    - metadata_json_parses
    - metadata_matches_recipe
  gate_b:
    metric: greedy_parity
    threshold: 0.9
    tolerance: 0.05
publish:
  hf_target_namespace: kevinqz
  repo_name: Qwen3-0.6B-CoreAI
  collection: CoreAI · Apple on-device
  variant: int8
catalog:
  name: Qwen3 0.6B (fabric, int8)
  family: Qwen
  context_length: 8192
  languages:
  - en
  - zh
  bundle_kind: llm
  min_os:
    macos: '27.0'
    ios: '27.0'
  runtime_facts:
    stock_runtime: true
    custom_kernel: false
    patch_required: false
    aot_required: false
  capabilities:
  - chat
  - text-generation
  modalities:
    input:
    - text
    output:
    - text
  parameters: 0.6B
  architecture: transformer
  runner: CoreAIRunner
  tokenizer_required: true
  processor_required: false
status: draft
notes: 'HIGH-FIDELITY int8 variant of qwen3-0.6b — the same Qwen3-0.6B converted with the
  int8 absmax/per-block-32 config (quant/int8_absmax_perblock32.yaml) instead of Apple''s
  int4 registry preset. VALIDATED ON REAL HARDWARE 2026-07-03 (M4 Max, macOS 26.6): greedy_parity
  vs the fp16 reference = 95.8% argmax / 100% top-5 / 100% margin-gated — essentially lossless,
  where the int4 sibling measures 79% argmax. int4 is the size-optimized tier; this int8 is
  the high-fidelity tier. Both publish into the one Qwen3-0.6B-CoreAI repo (int4/ + int8/
  subdirs). See docs/parity-protocol.md and quant/int8_absmax_perblock32.yaml.'
