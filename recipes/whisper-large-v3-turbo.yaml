# coreai-fabric recipe — Whisper large-v3-turbo (audio seed recipe)
# Status: draft. This recipe has NOT been converted yet — conversion requires
# macOS + the Apple Core AI toolchain (apple/coreai-torch). See AGENTS.md.
# Upstream metadata resolved live from the HF API on 2026-07-03.
id: whisper-large-v3-turbo
upstream:
  hf_repo: openai/whisper-large-v3-turbo
  revision: 41f01f3fe87f28c78e2fbf8b568835947dd65ed9
  license: mit
  license_terms: permissive
  pipeline_tag: automatic-speech-recognition
  size_bytes: 5096208915
conversion:
  tool: models/whisper/export.py
  min_tool_version: 0.4.1
  args:
    model: openai/whisper-large-v3-turbo
    dtype: float16
  quantization: none
  precision: float16
expected:
  bundle_files:
  - metadata.json
  - main.mlirb
  - main.hash
parity:
  gate_a:
    checks:
    - bundle_files_present
    - metadata_json_parses
    - metadata_matches_recipe
  gate_b:
    metric: graph_output_cosine
    threshold: 0.999
    tolerance: 0.0005
publish:
  hf_target_namespace: coreai-community
  repo_name: whisper-large-v3-turbo-coreai
catalog:
  name: Whisper large-v3-turbo (fabric)
  family: Whisper
  capabilities:
  - speech-to-text
  modalities:
    input:
    - audio
    output:
    - transcript
  parameters: 809M
  architecture: transformer
  runner: unknown
  tokenizer_required: true
  processor_required: true
  aot_required: unknown
status: draft
notes: 'Converter reality (verified against the apple/coreai-models source, tag
  0.1.0, 2026-07-03): whisper is converted by the per-model PEP 723 script
  models/whisper/export.py (run via uv from a checkout; pins
  coreai-torch==0.4.1 and transformers==4.57.3). Verified flags: --model
  {openai/whisper-large-v3-turbo, openai/whisper-large-v3} --dtype
  {float16,bfloat16,float32} --output-dir --overwrite; output naming is
  script-owned, so fabric prints the manual invocation instead of driving it
  (place the result at build/whisper-large-v3-turbo/whisper-large-v3-turbo.aimodel
  and run verify). License note: the upstream HF repo declares mit (verified
  via the HF API 2026-07-03); the catalog''s existing zoo-official conversion
  entry lists Apache-2.0 — fabric records the upstream repo''s own declaration.
  Runner is unknown until the first conversion is verified on-device (the zoo
  conversion uses CoreAITranscribe).'
