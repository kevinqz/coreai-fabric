# coreai-fabric recipe — Qwen3 0.6B (LLM seed recipe)
# Status: draft. This recipe has NOT been converted yet — conversion requires
# macOS + the Apple Core AI toolchain (apple/coreai-torch). See AGENTS.md.
# Upstream metadata resolved live from the HF API on 2026-07-03.
id: qwen3-0.6b
upstream:
  hf_repo: Qwen/Qwen3-0.6B
  revision: c1899de289a04d12100db370d81485cdf75e47ca
  license: apache-2.0
  license_terms: permissive
  pipeline_tag: text-generation
  size_bytes: 4522815806
conversion:
  tool: coreai-torch
  args: {}
  quantization: none
  precision: fp16
  compute_units: all
expected:
  bundle_files:
  - metadata.json
parity:
  gate_a:
    checks:
    - bundle_files_present
    - metadata_json_parses
    - metadata_matches_recipe
  gate_b:
    metric: per_token_logit_cosine
    threshold: 0.999
    tolerance: 0.0005
    greedy_token_exact: true
publish:
  hf_target_namespace: coreai-community
  repo_name: qwen3-0.6b-coreai
catalog:
  name: Qwen3 0.6B (fabric)
  family: Qwen
  capabilities:
  - chat
  - text-generation
  modalities:
    input:
    - text
    output:
    - text
  parameters: 0.6B
  architecture: transformer
  runner: unknown
  tokenizer_required: true
  processor_required: false
  aot_required: unknown
status: draft
notes: Fabric-native fp16 conversion of Qwen3-0.6B. Distinct from the catalog's
  existing official-qwen3-0-6b entry (int4 palettized, converted via Apple's
  official recipe by the zoo). Runner and AOT requirements are unknown until
  the first conversion is verified on-device.
