id: vla-jepa-pretrain
upstream:
  hf_repo: lerobot/VLA-JEPA-Pretrain
  revision: e946c3e5b538d760f4b4ff239d1b1c12090c041d
  license: apache-2.0
  license_terms: permissive
  pipeline_tag: robotics
  size_bytes: 6163226360
conversion:
  tool: models/vla_jepa/export.py
  min_tool_version: 0.4.1
  args:
    policy: lerobot/VLA-JEPA-Pretrain
    dtype: bfloat16
  quantization: none
  precision: bfloat16
  action:
    embodiment: droid_pretrain
    robot_required: true
    action_space:
      dim: 7
      max_action_dim: 7
      max_state_dim: 8
      chunk_size: 7
      convention: droid_7dof_action_with_gripper
    sampling:
      kind: flow_matching
      num_steps: 4
      solver: euler
    normalization:
      stats_source: policy_preprocessor.json + policy_postprocessor.json (upstream); action
        normalization is MIN_MAX, state normalization is MEAN_STD, visual normalization is
        IDENTITY per VLAJEPAConfig.
    graphs:
    - name: action_denoise_step
expected:
  bundle_files:
  - metadata.json
  - main.mlirb
  - main.hash
parity:
  gate_a:
    checks:
    - bundle_files_present
    - metadata_json_parses
    - metadata_matches_recipe
  gate_b:
    metric: action_parity
    threshold: 0.995
    tolerance: 0.001
    max_action_mae: 0.05
    max_relative_action_mae: 0.1
publish:
  hf_target_namespace: kevinqz
  repo_name: VLA-JEPA-Pretrain-CoreAI
  collection: CoreAI · Apple on-device
catalog:
  name: VLA-JEPA Pretrain (fabric)
  family: LeRobot
  bundle_kind: action
  min_os:
    macos: '27.0'
    ios: '27.0'
  runtime_facts:
    stock_runtime: true
    custom_kernel: false
    patch_required: false
    aot_required: false
  capabilities:
  - vision-language-action
  - robotics
  modalities:
    input:
    - image
    - text
    - state
    output:
    - action
  parameters: 3B
  architecture: transformer
  runner: CoreAIRunner
  tokenizer_required: true
  processor_required: true
status: verified
notes: 'VLA-JEPA pretrained policy from the LeRobot VLA-JEPA collection. Upstream card: VLA-JEPA
  combines Qwen3-VL-2B-Instruct, V-JEPA2, and a flow-matching DiT action head; inference uses
  Qwen + the action head, while the JEPA world model is training-only. Contract from config:
  two 224x224 RGB exterior-left camera streams (observation.images.exterior_1_left, observation.images.exterior_2_left),
  no state input declared in input_features for this checkpoint, action_dim=7, chunk_size=7,
  n_action_steps=7, num_inference_timesteps=4, jepa_encoder_name=facebook/vjepa2-vitl-fpc64-256.
  Current verified asset covers the `action_denoise_step` lane only: real action-head weights
  were exported, lowered, and measured with Gate B action_parity. Host-owned Qwen preprocessing
  and `qwen_context` export remain a separate lane; see docs/vla-jepa-export-runbook.md.'
